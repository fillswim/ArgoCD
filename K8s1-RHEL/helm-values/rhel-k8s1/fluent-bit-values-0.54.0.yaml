---
config:
  outputs: |
    [OUTPUT]
        Name loki
        Match kube.*
        Host loki.fillswim.local
        port 443
        tls on
        tls.verify off
        tenant_id fillswim
        labels job=fluentbit,env=test,app=k8s,cluster=alma-k8s2,subsystem=cluster